# CNT3054
Code for CNT 3054

## Setup scripts
- `requirmentsmac.txt`: macOS setup script (Homebrew, Git, VS Code, uv).
- `requirmentswin.txt`: Windows PowerShell setup script (Git, VS Code, uv).

Both scripts include checks that skip installation when tools are already installed.
